<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Training & Development | Kenbridge Christian School</title>
<link rel="stylesheet" href="../css/style.css">
<style>
:root{--tr-primary:#174ea6;--tr-secondary:#0b8043;--tr-warning:#f9ab00;--tr-danger:#d93025;--tr-bg:#f5f7fb;--tr-card:#fff;--tr-text:#172033;--tr-muted:#6b7280;--tr-border:#e5e7eb}
body{background:var(--tr-bg)}
.training-wrap{max-width:1400px;margin:auto;padding:24px}
.training-hero{background:linear-gradient(135deg,#174ea6,#12366f);color:#fff;border-radius:18px;padding:28px;margin-bottom:22px;display:flex;justify-content:space-between;gap:20px;align-items:center}
.training-hero h1{margin:0 0 8px;font-size:30px}.training-hero p{margin:0;opacity:.9}
.training-actions{display:flex;gap:10px;flex-wrap:wrap}
.tr-btn{border:0;border-radius:9px;padding:11px 16px;cursor:pointer;font-weight:700}
.tr-btn-primary{background:#fff;color:#174ea6}.tr-btn-dark{background:#174ea6;color:#fff}.tr-btn-green{background:#0b8043;color:#fff}.tr-btn-danger{background:#d93025;color:#fff}.tr-btn-light{background:#eef3fb;color:#174ea6}
.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:15px;margin-bottom:22px}
.stat{background:var(--tr-card);border:1px solid var(--tr-border);border-radius:14px;padding:19px;display:flex;align-items:center;gap:15px}
.stat-icon{width:48px;height:48px;border-radius:12px;background:#eaf1ff;display:grid;place-items:center;font-size:23px}
.stat strong{font-size:25px;display:block}.stat span{color:var(--tr-muted);font-size:13px}
.grid{display:grid;grid-template-columns:2fr 1fr;gap:20px}
.card{background:var(--tr-card);border:1px solid var(--tr-border);border-radius:15px;padding:20px;margin-bottom:20px}
.card h2{font-size:19px;margin:0 0 15px;color:var(--tr-text)}
.section-head{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:15px}
.section-head h2{margin:0}.muted{color:var(--tr-muted)}
.session{border:1px solid var(--tr-border);border-radius:12px;padding:15px;margin-bottom:12px}
.session:last-child{margin-bottom:0}.session-top{display:flex;justify-content:space-between;gap:12px}
.session h3{margin:0 0 6px;font-size:16px}.session p{margin:4px 0;color:var(--tr-muted);font-size:13px}
.badge{display:inline-block;padding:4px 8px;border-radius:20px;font-size:11px;font-weight:700;background:#eef2f7;color:#46505f}
.badge-green{background:#e7f6ed;color:#08783e}.badge-blue{background:#e8f0ff;color:#174ea6}.badge-yellow{background:#fff5d9;color:#8a6500}.badge-red{background:#fde9e7;color:#b3261e}
.session-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:12px}
.upcoming-empty{text-align:center;padding:30px;color:var(--tr-muted)}
.program-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}
.program{border:1px solid var(--tr-border);border-radius:12px;padding:15px}
.program h3{font-size:15px;margin:0 0 7px}.program p{font-size:13px;color:var(--tr-muted);margin:0 0 10px}
.progress{height:8px;background:#edf0f4;border-radius:20px;overflow:hidden}.progress i{display:block;height:100%;background:#174ea6}
.training-list{display:flex;flex-direction:column;gap:9px}.training-row{border:1px solid var(--tr-border);border-radius:10px;padding:12px}
.training-row strong{display:block;font-size:14px}.training-row small{color:var(--tr-muted)}
.notice{padding:12px;border-radius:10px;background:#eef5ff;margin-bottom:9px;font-size:13px}
.notice.unread{border-left:4px solid #174ea6}
.material{display:flex;justify-content:space-between;gap:12px;align-items:center;border-bottom:1px solid var(--tr-border);padding:12px 0}
.material:last-child{border-bottom:0}.material-info strong{display:block}.material-info small{color:var(--tr-muted)}
.cert{border:1px solid var(--tr-border);border-radius:12px;padding:14px;margin-bottom:10px}
.cert strong{display:block}.cert small{color:var(--tr-muted)}
.calendar{display:grid;grid-template-columns:repeat(7,1fr);gap:5px}
.cal-day{background:#f8fafc;border:1px solid #edf0f4;min-height:65px;padding:6px;border-radius:7px;font-size:11px}
.cal-day b{display:block;font-size:12px}.cal-event{background:#e8f0ff;color:#174ea6;padding:3px;border-radius:4px;margin-top:4px}
.topic-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
.topic{border:1px solid var(--tr-border);border-radius:10px;padding:12px}.topic b{display:block}.topic small{color:var(--tr-muted)}
.search-row{display:flex;gap:10px;flex-wrap:wrap;margin-bottom:15px}
.search-row input,.search-row select,.modal input,.modal select,.modal textarea{border:1px solid #d8dde6;border-radius:8px;padding:10px;background:#fff}
.search-row input{flex:1;min-width:180px}
.tabs{display:flex;gap:6px;overflow:auto;margin-bottom:15px}.tab{padding:9px 13px;border:1px solid var(--tr-border);background:#fff;border-radius:8px;cursor:pointer;white-space:nowrap}.tab.active{background:#174ea6;color:#fff}
.hidden{display:none!important}
.modal{position:fixed;inset:0;background:rgba(0,0,0,.55);display:none;align-items:center;justify-content:center;padding:20px;z-index:9999}.modal.show{display:flex}
.modal-box{background:#fff;border-radius:15px;width:min(650px,100%);max-height:90vh;overflow:auto;padding:22px}.modal-head{display:flex;justify-content:space-between;align-items:center}.modal-head h2{margin:0}.close{border:0;background:#eee;border-radius:50%;width:34px;height:34px;cursor:pointer}
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field{display:flex;flex-direction:column;gap:5px}.field.full{grid-column:1/-1}.field label{font-size:13px;font-weight:700}.modal textarea{min-height:100px;resize:vertical}.modal-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:18px}
.loading{text-align:center;padding:25px;color:var(--tr-muted)}
@media(max-width:1000px){.stats{grid-template-columns:repeat(2,1fr)}.grid{grid-template-columns:1fr}}
@media(max-width:650px){.training-wrap{padding:12px}.training-hero{padding:20px;display:block}.training-actions{margin-top:15px}.stats{grid-template-columns:1fr 1fr}.program-grid,.topic-grid,.form-grid{grid-template-columns:1fr}.field.full{grid-column:auto}.calendar{grid-template-columns:repeat(7,minmax(35px,1fr));overflow:auto}.session-top{display:block}}
</style>
</head>

<body>

<header>
<div class="container">
<h1>Kenbridge Christian School Staff Portal</h1>
<nav>
<a href="staff-dashboard.html">Dashboard</a>
<a href="students.html">Students</a>
<a href="attendance.html">Attendance</a>
<a href="announcements.html">Announcements</a>
<a href="articles.html">Articles</a>
<a href="reports.html">Reports</a>
<a href="files.html">Files</a>
<a href="calendar.html">Calendar</a>
<a href="messages.html">Messages</a>
<a href="tasks.html">Tasks</a>
<a href="leave.html">Leave</a>
<a href="payroll.html">Payroll</a>
<a href="training.html" class="active">Training</a>
<a href="notifications.html">Notifications</a>
<a href="profile.html">My Profile</a>
</nav>
</div>
</header>

<main class="training-wrap">

<section class="training-hero">
<div>
<h1>Training & Development</h1>
<p>Your professional development centre for workshops, live sessions, training packages, groups and certificates.</p>
</div>
<div class="training-actions">
<button class="tr-btn tr-btn-primary" onclick="loadEverything()">↻ Refresh</button>
<button id="adminBtn" class="tr-btn tr-btn-green hidden" onclick="openAdmin()">⚙ Training Administration</button>
</div>
</section>

<section class="stats">
<div class="stat"><div class="stat-icon">📚</div><div><strong id="statPrograms">0</strong><span>Active Programs</span></div></div>
<div class="stat"><div class="stat-icon">🎓</div><div><strong id="statMine">0</strong><span>My Active Training</span></div></div>
<div class="stat"><div class="stat-icon">🏆</div><div><strong id="statCertificates">0</strong><span>Certificates</span></div></div>
<div class="stat"><div class="stat-icon">🔔</div><div><strong id="statNotifications">0</strong><span>Unread Alerts</span></div></div>
</section>

<div class="tabs">
<button class="tab active" data-tab="overview">Overview</button>
<button class="tab" data-tab="programs">Training Programs</button>
<button class="tab" data-tab="sessions">Live Sessions</button>
<button class="tab" data-tab="groups">Workshops & Groups</button>
<button class="tab" data-tab="materials">Training Packages</button>
<button class="tab" data-tab="mine">My Training</button>
<button class="tab" data-tab="certificates">Certificates</button>
<button class="tab" data-tab="calendar">Calendar</button>
<button class="tab" data-tab="notifications">Notifications</button>
</div>

<section id="tab-overview" class="tab-panel">
<div class="grid">
<div>
<div class="card">
<div class="section-head"><h2>📅 Upcoming Training Sessions</h2><button class="tr-btn tr-btn-light" onclick="switchTab('sessions')">View All</button></div>
<div id="upcomingSessions"><div class="loading">Loading sessions...</div></div>
</div>

<div class="card">
<div class="section-head"><h2>🎓 Professional Development</h2></div>
<div class="topic-grid">
<div class="topic"><b>👨‍🏫 Classroom Management</b><small>Build effective, respectful and productive learning environments.</small></div>
<div class="topic"><b>📖 Lesson Planning</b><small>Improve preparation, teaching strategies and lesson delivery.</small></div>
<div class="topic"><b>🛡️ Child Protection</b><small>Understand safeguarding, student welfare and professional responsibility.</small></div>
<div class="topic"><b>✝️ Christian Education</b><small>Strengthen Christian values and character formation in teaching.</small></div>
<div class="topic"><b>💻 Digital Teaching</b><small>Use technology and digital resources effectively in learning.</small></div>
<div class="topic"><b>📝 Assessment</b><small>Improve assessment, feedback, learner support and progress tracking.</small></div>
</div>
</div>
</div>

<div>
<div class="card">
<div class="section-head"><h2>🔔 Training Alerts</h2><button class="tr-btn tr-btn-light" onclick="switchTab('notifications')">All</button></div>
<div id="overviewNotifications"><div class="loading">Loading...</div></div>
</div>

<div class="card">
<div class="section-head"><h2>🏆 My Certificates</h2></div>
<div id="overviewCertificates"><div class="loading">Loading...</div></div>
</div>
</div>
</div>
</section>

<section id="tab-programs" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>Training Programs</h2><button id="addProgramBtn" class="tr-btn tr-btn-dark hidden" onclick="openProgramModal()">+ Create Training</button></div>
<div class="search-row">
<input id="programSearch" placeholder="Search training programs..." oninput="loadPrograms()">
<select id="programCategory" onchange="loadPrograms()"><option value="">All Categories</option><option>Classroom Management</option><option>Child Protection</option><option>Christian Education</option><option>Digital Skills</option><option>Assessment</option><option>Leadership</option><option>Professional Development</option><option>Other</option></select>
</div>
<div id="programList" class="program-grid"><div class="loading">Loading...</div></div>
</div>
</section>

<section id="tab-sessions" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>🎥 Live Training & Meetings</h2><button id="addSessionBtn" class="tr-btn tr-btn-dark hidden" onclick="openSessionModal()">+ Schedule Session</button></div>
<p class="muted">Join scheduled Zoom, Google Meet, online, physical or hybrid staff-development sessions.</p>
<div id="sessionList"><div class="loading">Loading...</div></div>
</div>
</section>

<section id="tab-groups" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>👥 Workshops & Training Groups</h2></div>
<div id="groupList"><div class="loading">Select a training program to view its groups.</div></div>
</div>
</section>

<section id="tab-materials" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>📚 Training Packages & Materials</h2><button id="addMaterialBtn" class="tr-btn tr-btn-dark hidden" onclick="openMaterialModal()">+ Add Material</button></div>
<div class="search-row"><input id="materialSearch" placeholder="Search training packages..." oninput="loadMaterials()"></div>
<div id="materialList"><div class="loading">Loading...</div></div>
</div>
</section>

<section id="tab-mine" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>📈 My Training</h2></div>
<div id="myTraining"><div class="loading">Loading...</div></div>
</div>
</section>

<section id="tab-certificates" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>🏆 My Certificates</h2></div>
<div id="certificateList"><div class="loading">Loading...</div></div>
</div>
</section>

<section id="tab-calendar" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>🗓️ Training Calendar</h2></div>
<div id="calendar" class="calendar"></div>
</div>
</section>

<section id="tab-notifications" class="tab-panel hidden">
<div class="card">
<div class="section-head"><h2>🔔 Training Notifications</h2><button class="tr-btn tr-btn-light" onclick="readAllNotifications()">Mark All Read</button></div>
<div id="notificationList"><div class="loading">Loading...</div></div>
</div>
</section>

</main>

<!-- PROGRAM MODAL -->
<div id="programModal" class="modal">
<div class="modal-box">
<div class="modal-head"><h2>Create Training Program</h2><button class="close" onclick="closeModal('programModal')">×</button></div>
<form id="programForm">
<div class="form-grid">
<div class="field full"><label>Training Title *</label><input name="title" required></div>
<div class="field"><label>Category</label><select name="category"><option>Classroom Management</option><option>Child Protection</option><option>Christian Education</option><option>Digital Skills</option><option>Assessment</option><option>Leadership</option><option>Professional Development</option><option>Other</option></select></div>
<div class="field"><label>Trainer</label><input name="trainer_name"></div>
<div class="field"><label>Start Date</label><input name="start_date" type="datetime-local"></div>
<div class="field"><label>End Date</label><input name="end_date" type="datetime-local"></div>
<div class="field"><label>Organization</label><input name="trainer_organization"></div>
<div class="field"><label>Duration (minutes)</label><input name="duration_minutes" type="number" min="1"></div>
<div class="field"><label>Required Training</label><select name="required"><option value="false">No</option><option value="true">Yes</option></select></div>
<div class="field full"><label>Description</label><textarea name="description"></textarea></div>
</div>
<div class="modal-actions"><button type="button" class="tr-btn tr-btn-light" onclick="closeModal('programModal')">Cancel</button><button class="tr-btn tr-btn-dark">Create</button></div>
</form>
</div>
</div>

<!-- SESSION MODAL -->
<div id="sessionModal" class="modal">
<div class="modal-box">
<div class="modal-head"><h2>Schedule Training Session</h2><button class="close" onclick="closeModal('sessionModal')">×</button></div>
<form id="sessionForm">
<div class="form-grid">
<div class="field full"><label>Training Program *</label><select name="program_id" id="sessionProgram" required></select></div>
<div class="field full"><label>Session Title *</label><input name="title" required></div>
<div class="field"><label>Date & Time *</label><input name="session_date" type="datetime-local" required></div>
<div class="field"><label>End Time</label><input name="end_date" type="datetime-local"></div>
<div class="field"><label>Meeting Type</label><select name="meeting_type"><option>ZOOM</option><option>GOOGLE_MEET</option><option>ONLINE</option><option>PHYSICAL</option><option>HYBRID</option></select></div>
<div class="field"><label>Location</label><input name="location" placeholder="Venue / room"></div>
<div class="field full"><label>Meeting URL</label><input name="meeting_url" placeholder="Zoom or meeting link"></div>
<div class="field"><label>Meeting ID</label><input name="meeting_id"></div>
<div class="field"><label>Meeting Password</label><input name="meeting_password"></div>
<div class="field"><label>Trainer</label><input name="trainer_name"></div>
<div class="field full"><label>Description</label><textarea name="description"></textarea></div>
</div>
<div class="modal-actions"><button type="button" class="tr-btn tr-btn-light" onclick="closeModal('sessionModal')">Cancel</button><button class="tr-btn tr-btn-dark">Schedule</button></div>
</form>
</div>
</div>

<!-- MATERIAL MODAL -->
<div id="materialModal" class="modal">
<div class="modal-box">
<div class="modal-head"><h2>Add Training Material</h2><button class="close" onclick="closeModal('materialModal')">×</button></div>
<form id="materialForm">
<div class="form-grid">
<div class="field full"><label>Title *</label><input name="title" required></div>
<div class="field"><label>Program</label><select name="program_id" id="materialProgram"></select></div>
<div class="field"><label>Material Type</label><select name="material_type"><option>DOCUMENT</option><option>PRESENTATION</option><option>VIDEO</option><option>GUIDE</option><option>MANUAL</option><option>LINK</option><option>OTHER</option></select></div>
<div class="field full"><label>External Link</label><input name="external_url" placeholder="Optional resource or video URL"></div>
<div class="field full"><label>Description</label><textarea name="description"></textarea></div>
</div>
<div class="modal-actions"><button type="button" class="tr-btn tr-btn-light" onclick="closeModal('materialModal')">Cancel</button><button class="tr-btn tr-btn-dark">Add Material</button></div>
</form>
</div>
</div>

<script src="../js/portal-auth.js"></script>
<script src="../js/portal-api.js"></script>
<script>
"use strict";

const BASE=(window.API&&API.API_BASE)||"https://kenbridge-christian-school.onrender.com";
const APIROOT=BASE+"/api/training";

let isAdmin=false;
let programs=[];
let sessions=[];
let materials=[];
let certificates=[];
let notifications=[];

function token(){
 return (window.API&&typeof API.getToken==="function")
  ? API.getToken()
  : localStorage.getItem("kenbridgeAccessToken")||
    localStorage.getItem("access_token")||
    localStorage.getItem("kenbridge_access_token")||
    sessionStorage.getItem("kenbridgeAccessToken")||"";
}

async function api(path,options={}){
 const headers=options.headers||{};
 headers.Authorization="Bearer "+token();
 if(options.body&&typeof options.body==="object"){
  headers["Content-Type"]="application/json";
  options.body=JSON.stringify(options.body);
 }
 const r=await fetch(APIROOT+path,{...options,headers});
 let data={};
 try{data=await r.json()}catch(e){}
 if(!r.ok)throw new Error(data.message||"Request failed.");
 return data;
}

function esc(v){
 return String(v??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
}

function fmt(v){
 if(!v)return "Not specified";
 const d=new Date(v);
 return isNaN(d)?"Not specified":d.toLocaleString([],{
  weekday:"short",year:"numeric",month:"short",day:"numeric",
  hour:"numeric",minute:"2-digit"
 });
}

function closeModal(id){document.getElementById(id).classList.remove("show")}
function openModal(id){document.getElementById(id).classList.add("show")}

function switchTab(name){
 document.querySelectorAll(".tab").forEach(x=>x.classList.toggle("active",x.dataset.tab===name));
 document.querySelectorAll(".tab-panel").forEach(x=>x.classList.add("hidden"));
 document.getElementById("tab-"+name).classList.remove("hidden");
 if(name==="programs")loadPrograms();
 if(name==="sessions")loadSessions();
 if(name==="groups")loadGroups();
 if(name==="materials")loadMaterials();
 if(name==="mine")loadMine();
 if(name==="certificates")loadCertificates();
 if(name==="calendar")buildCalendar();
 if(name==="notifications")loadNotifications();
}

document.querySelectorAll(".tab").forEach(x=>x.onclick=()=>switchTab(x.dataset.tab));

async function loadDashboard(){
 try{
  const d=await api("/dashboard");
  const s=d.statistics||{};
  document.getElementById("statPrograms").textContent=s.activePrograms||0;
  document.getElementById("statMine").textContent=s.myActiveTraining||0;
  document.getElementById("statCertificates").textContent=s.certificates||0;
  document.getElementById("statNotifications").textContent=s.unreadNotifications||0;
  renderSessions(d.upcoming||[],"upcomingSessions");
 }catch(e){
  document.getElementById("upcomingSessions").innerHTML='<div class="upcoming-empty">'+esc(e.message)+'</div>';
 }
}

async function loadPrograms(){
 try{
  const q=new URLSearchParams();
  const s=document.getElementById("programSearch")?.value.trim();
  const c=document.getElementById("programCategory")?.value;
  if(s)q.set("search",s);
  if(c)q.set("category",c);
  const d=await api("/programs?"+q.toString());
  programs=d.programs||[];
  document.getElementById("programList").innerHTML=programs.length?programs.map(p=>`
   <div class="program">
    <span class="badge ${p.status==="ONGOING"?"badge-green":p.status==="UPCOMING"?"badge-blue":"badge"}">${esc(p.status)}</span>
    ${p.required?'<span class="badge badge-yellow">REQUIRED</span>':''}
    <h3>${esc(p.title)}</h3>
    <p>${esc(p.description||"No description provided.")}</p>
    <small class="muted">👨‍🏫 ${esc(p.trainer_name||"Trainer TBA")}</small><br>
    <small class="muted">📅 ${fmt(p.start_date)}</small>
    <div class="session-actions">
     <button class="tr-btn tr-btn-light" onclick="viewProgram('${p.id}')">View Details</button>
     ${p.status!=="COMPLETED"&&p.status!=="CANCELLED"?`<button class="tr-btn tr-btn-green" onclick="enroll('${p.id}')">Enroll</button>`:""}
    </div>
   </div>`).join(""):'<div class="loading">No training programs found.</div>';
 }catch(e){document.getElementById("programList").innerHTML='<div class="loading">'+esc(e.message)+'</div>'}
}

async function viewProgram(id){
 try{
  const d=await api("/programs/"+id);
  const p=d.program;
  alert(
   p.title+"\n\n"+
   (p.description||"No description.")+
   "\n\nTrainer: "+(p.trainer_name||"TBA")+
   "\nStart: "+fmt(p.start_date)+
   "\nSessions: "+(d.sessions||[]).length+
   "\nGroups: "+(d.groups||[]).length+
   "\nMaterials: "+(d.materials||[]).length
  );
 }catch(e){alert(e.message)}
}

async function enroll(id){
 if(!confirm("Enroll in this training program?"))return;
 try{await api("/enroll",{method:"POST",body:{program_id:id}});alert("You have been enrolled successfully.");loadEverything()}
 catch(e){alert(e.message)}
}

function renderSessions(list,target){
 const el=document.getElementById(target);
 if(!list.length){el.innerHTML='<div class="upcoming-empty">No training sessions scheduled.</div>';return}
 el.innerHTML=list.map(s=>{
  const online=["ZOOM","GOOGLE_MEET","ONLINE"].includes(String(s.meeting_type).toUpperCase());
  return `<div class="session">
   <div class="session-top">
    <div><span class="badge ${s.status==="LIVE"?"badge-green":online?"badge-blue":"badge"}">${esc(s.status)}</span>
    <span class="badge">${esc(s.meeting_type)}</span>
    <h3>${esc(s.title)}</h3>
    <p>📚 ${esc(s.program_title||"Training")}</p>
    <p>📅 ${fmt(s.session_date)}</p>
    <p>👨‍🏫 ${esc(s.trainer_name||"Trainer TBA")}</p>
    ${s.location?`<p>📍 ${esc(s.location)}</p>`:""}
    ${s.meeting_id?`<p>🆔 Meeting ID: ${esc(s.meeting_id)}</p>`:""}
    </div>
   </div>
   ${s.description?`<p>${esc(s.description)}</p>`:""}
   <div class="session-actions">
    ${s.meeting_url?`<button class="tr-btn tr-btn-green" onclick="joinMeeting('${esc(s.meeting_url)}')">🎥 Join Meeting</button>`:""}
    ${s.materials_url?`<button class="tr-btn tr-btn-light" onclick="openLink('${esc(s.materials_url)}')">📚 Materials</button>`:""}
    ${s.recording_url?`<button class="tr-btn tr-btn-light" onclick="openLink('${esc(s.recording_url)}')">▶ Recording</button>`:""}
    <button class="tr-btn tr-btn-light" onclick="markAttendance('${s.id}')">✓ Attendance</button>
   </div>
  </div>`
 }).join("");
}

async function loadSessions(){
 try{
  const d=await api("/sessions");
  sessions=d.sessions||[];
  renderSessions(sessions,"sessionList");
 }catch(e){document.getElementById("sessionList").innerHTML='<div class="loading">'+esc(e.message)+'</div>'}
}

function joinMeeting(url){
 if(!url)return;
 window.open(url,"_blank","noopener,noreferrer");
}

function openLink(url){window.open(url,"_blank","noopener,noreferrer")}

async function markAttendance(id){
 try{
  await api("/attendance",{method:"POST",body:{session_id:id,status:"PRESENT",joined_at:new Date().toISOString()}});
  alert("Attendance recorded.");
 }catch(e){alert(e.message)}
}

async function loadMine(){
 try{
  const d=await api("/my-training");
  const list=d.training||[];
  document.getElementById("myTraining").innerHTML=list.length?list.map(x=>`
   <div class="training-row">
    <strong>${esc(x.title)}</strong>
    <small>${esc(x.category||"Training")} • ${esc(x.status)}</small>
    <div style="margin:9px 0" class="progress"><i style="width:${Number(x.progress||0)}%"></i></div>
    <small>${Number(x.progress||0)}% complete${x.group_name?" • Group: "+esc(x.group_name):""}</small>
   </div>`).join(""):'<div class="loading">You have no training assignments yet.</div>';
 }catch(e){document.getElementById("myTraining").innerHTML='<div class="loading">'+esc(e.message)+'</div>'}
}

async function loadGroups(){
 if(!programs.length)await loadPrograms();
 if(!programs.length){document.getElementById("groupList").innerHTML='<div class="loading">No training programs available.</div>';return}
 let html="";
 for(const p of programs){
  try{
   const d=await api("/groups/"+p.id);
   const groups=d.groups||[];
   if(groups.length)html+=`<div class="card" style="margin-bottom:12px"><h2>${esc(p.title)}</h2>`+
    groups.map(g=>`<div class="training-row"><strong>👥 ${esc(g.name)}</strong><small>${esc(g.facilitator_name||"Facilitator TBA")} • ${g.member_count||0} members</small></div>`).join("")+
    `</div>`;
  }catch(e){}
 }
 document.getElementById("groupList").innerHTML=html||'<div class="loading">No workshop groups have been assigned yet.</div>';
}

async function loadMaterials(){
 try{
  const q=new URLSearchParams();
  const s=document.getElementById("materialSearch")?.value.trim();
  if(s)q.set("search",s);
  const d=await api("/materials?"+q.toString());
  materials=d.materials||[];
  document.getElementById("materialList").innerHTML=materials.length?materials.map(m=>`
   <div class="material">
    <div class="material-info">
     <strong>📚 ${esc(m.title)}</strong>
     <small>${esc(m.program_title||"General Training")} • ${esc(m.material_type||"DOCUMENT")}</small>
     ${m.description?`<br><small>${esc(m.description)}</small>`:""}
    </div>
    <div>
     ${m.external_url?`<button class="tr-btn tr-btn-light" onclick="openLink('${esc(m.external_url)}')">Open</button>`:""}
     ${m.file_path?`<button class="tr-btn tr-btn-light" onclick="openLink('${esc(m.file_path)}')">View</button>`:""}
    </div>
   </div>`).join(""):'<div class="loading">No training materials available.</div>';
 }catch(e){document.getElementById("materialList").innerHTML='<div class="loading">'+esc(e.message)+'</div>'}
}

async function loadCertificates(){
 try{
  const d=await api("/certificates");
  certificates=d.certificates||[];
  const html=certificates.length?certificates.map(c=>`
   <div class="cert">
    <strong>🏆 ${esc(c.title)}</strong>
    <small>${esc(c.program_title||"Training")} • Issued ${fmt(c.issued_date)}</small><br>
    <small>Certificate: ${esc(c.certificate_number)}</small>
    ${c.certificate_url?`<br><button class="tr-btn tr-btn-light" style="margin-top:8px" onclick="openLink('${esc(c.certificate_url)}')">View Certificate</button>`:""}
   </div>`).join(""):'<div class="loading">No certificates have been issued yet.</div>';
  document.getElementById("certificateList").innerHTML=html;
  document.getElementById("overviewCertificates").innerHTML=certificates.slice(0,3).map(c=>`<div class="training-row"><strong>🏆 ${esc(c.title)}</strong><small>${fmt(c.issued_date)}</small></div>`).join("")||'<div class="muted">No certificates yet.</div>';
 }catch(e){
  document.getElementById("certificateList").innerHTML='<div class="loading">'+esc(e.message)+'</div>';
  document.getElementById("overviewCertificates").innerHTML='<div class="muted">Unable to load certificates.</div>';
 }
}

async function loadNotifications(){
 try{
  const d=await api("/notifications");
  notifications=d.notifications||[];
  document.getElementById("notificationList").innerHTML=notifications.length?notifications.map(n=>`
   <div class="notice ${n.is_read?"":"unread"}">
    <strong>${esc(n.title)}</strong>
    <div>${esc(n.message)}</div>
    <small class="muted">${fmt(n.created_at)}</small>
    ${!n.is_read?`<br><button class="tr-btn tr-btn-light" style="margin-top:7px" onclick="readNotification('${n.id}')">Mark Read</button>`:""}
   </div>`).join(""):'<div class="loading">No training notifications.</div>';
  document.getElementById("overviewNotifications").innerHTML=notifications.slice(0,4).map(n=>`<div class="notice ${n.is_read?"":"unread"}"><strong>${esc(n.title)}</strong><br><small>${esc(n.message)}</small></div>`).join("")||'<div class="muted">No alerts.</div>';
 }catch(e){document.getElementById("notificationList").innerHTML='<div class="loading">'+esc(e.message)+'</div>'}
}

async function readNotification(id){
 try{await api("/notifications/"+id+"/read",{method:"PATCH"});loadNotifications();loadDashboard()}
 catch(e){alert(e.message)}
}

async function readAllNotifications(){
 try{await api("/notifications/read-all",{method:"PATCH"});loadNotifications();loadDashboard()}
 catch(e){alert(e.message)}
}

function buildCalendar(){
 const el=document.getElementById("calendar");
 const now=new Date(),year=now.getFullYear(),month=now.getMonth();
 const first=new Date(year,month,1).getDay(),days=new Date(year,month+1,0).getDate();
 const names=["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];
 let html=names.map(x=>`<div class="cal-day"><b>${x}</b></div>`).join("");
 for(let i=0;i<first;i++)html+='<div class="cal-day"></div>';
 for(let day=1;day<=days;day++){
  const events=sessions.filter(s=>{
   const d=new Date(s.session_date);
   return d.getFullYear()===year&&d.getMonth()===month&&d.getDate()===day;
  });
  html+=`<div class="cal-day"><b>${day}</b>${events.map(e=>`<div class="cal-event">${esc(e.title)}</div>`).join("")}</div>`;
 }
 el.innerHTML=html;
}

async function openProgramModal(){openModal("programModal")}

async function openSessionModal(){
 if(!programs.length)await loadPrograms();
 document.getElementById("sessionProgram").innerHTML=programs.map(p=>`<option value="${p.id}">${esc(p.title)}</option>`).join("");
 openModal("sessionModal");
}

async function openMaterialModal(){
 if(!programs.length)await loadPrograms();
 document.getElementById("materialProgram").innerHTML='<option value="">General</option>'+programs.map(p=>`<option value="${p.id}">${esc(p.title)}</option>`).join("");
 openModal("materialModal");
}

async function openAdmin(){
 switchTab("programs");
}

document.getElementById("programForm").onsubmit=async e=>{
 e.preventDefault();
 const f=new FormData(e.target),b=Object.fromEntries(f.entries());
 b.required=b.required==="true";
 try{
  await api("/programs",{method:"POST",body:b});
  closeModal("programModal");e.target.reset();await loadPrograms();await loadDashboard();alert("Training program created.");
 }catch(x){alert(x.message)}
};

document.getElementById("sessionForm").onsubmit=async e=>{
 e.preventDefault();
 const f=new FormData(e.target),b=Object.fromEntries(f.entries());
 try{
  await api("/sessions",{method:"POST",body:b});
  closeModal("sessionModal");e.target.reset();await loadSessions();await loadDashboard();alert("Training session scheduled.");
 }catch(x){alert(x.message)}
};

document.getElementById("materialForm").onsubmit=async e=>{
 e.preventDefault();
 const f=new FormData(e.target),b=Object.fromEntries(f.entries());
 if(!b.program_id)delete b.program_id;
 try{
  await api("/materials",{method:"POST",body:b});
  closeModal("materialModal");e.target.reset();loadMaterials();alert("Training material added.");
 }catch(x){alert(x.message)}
};

async function detectAdmin(){
 try{
  const u=JSON.parse(localStorage.getItem("kenbridgeUser")||"{}");
  const role=String(u.role||localStorage.getItem("kenbridgeUserRole")||"").toUpperCase();
  isAdmin=role==="ADMIN";
 }catch(e){}
 if(isAdmin){
  document.getElementById("adminBtn").classList.remove("hidden");
  ["addProgramBtn","addSessionBtn","addMaterialBtn"].forEach(id=>document.getElementById(id).classList.remove("hidden"));
 }
}

async function loadEverything(){
 await detectAdmin();
 await Promise.all([
  loadDashboard(),
  loadPrograms(),
  loadSessions(),
  loadMine(),
  loadCertificates(),
  loadNotifications(),
  loadMaterials()
 ]);
 buildCalendar();
}

window.addEventListener("load",loadEverything);
</script>

</body>
</html>
